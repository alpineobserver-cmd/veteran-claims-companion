import { LayoutDashboard, ClipboardList, BookOpen, FolderOpen, FileText, Files, User, Settings, Search, Bell, Menu, ShieldCheck } from "lucide-react";
const links=[["Dashboard",LayoutDashboard],["Claim Builder",ClipboardList],["Condition Library",BookOpen],["Evidence Library",FolderOpen],["Templates",FileText],["Forms",Files],["Profile",User],["Settings",Settings]] as const;
export function AppShell({children}:{children:React.ReactNode}){return <div className="shell"><aside className="sidebar"><div className="brand"><div className="brandmark"><ShieldCheck size={22}/></div><div><strong>Veteran Claims</strong><span>Companion</span></div></div><nav className="nav" aria-label="Primary navigation">{links.map(([label,Icon],i)=><a className={i===0?"active":""} href={i===0?"/":"#"} key={label}><Icon size={18}/><span>{label}</span></a>)}</nav><div className="sidefoot">Educational guidance.<br/>Your records stay yours.</div></aside><main className="main"><header className="topbar"><button className="iconbtn mobile-menu" aria-label="Open menu"><Menu size={19}/></button><label className="search"><Search size={18}/><input aria-label="Search resources" placeholder="Search conditions, evidence, templates, and forms…"/></label><div className="top-actions"><button className="iconbtn" aria-label="Notifications"><Bell size={18}/></button><div className="avatar" aria-label="Demo user">JD</div></div></header>{children}</main></div>}
