"use client";
import { useMemo,useState } from "react";
import { ArrowRight,Search,SlidersHorizontal } from "lucide-react";
import { formCategories,vaForms } from "@/lib/va-forms";
export function FormsLibrary(){const [query,setQuery]=useState("");const [category,setCategory]=useState("All");const shown=useMemo(()=>vaForms.filter(f=>(category==="All"||f.category===category)&&`${f.number} ${f.name} ${f.purpose} ${f.related.join(" ")}`.toLowerCase().includes(query.toLowerCase())),[query,category]);return <><div className="forms-tools"><label className="forms-search"><Search size={18}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search by form number or purpose…" aria-label="Search VA forms"/></label><div className="forms-filter"><SlidersHorizontal size={16}/><select value={category} onChange={e=>setCategory(e.target.value)} aria-label="Filter forms by category">{formCategories.map(x=><option key={x}>{x}</option>)}</select></div></div><div className="forms-count"><strong>{shown.length}</strong> {shown.length===1?"resource":"resources"}<span>Always download a fresh copy from VA.gov</span></div>{shown.length?<div className="forms-grid">{shown.map(f=><a className="form-card" href={`/forms/${f.slug}`} key={f.slug}><div className="form-card-top"><span className="form-number">{f.number}</span><span className="form-category">{f.category}</span></div><h2>{f.name}</h2><p>{f.purpose}</p><div className="form-status"><span>{f.status}</span><ArrowRight size={15}/></div></a>)}</div>:<div className="forms-empty"><h2>No matching form</h2><p>Try searching by a form number, “appeal,” “private records,” or “unemployability.”</p></div>}</>}
